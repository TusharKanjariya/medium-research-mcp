---
phase: quick-260708-h5g
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/contract.js
  - test/contract.test.js
  - servers/rss/server.js
  - test/rss.test.js
  - CLAUDE.md
  - package.json
autonomous: true
requirements: []

must_haves:
  truths:
    - "A numeric entity above U+FFFF (e.g. &#128512; or &#x1F680;) in any source's HTML decodes to the actual emoji in item.text, not a mangled surrogate half"
    - "An out-of-range numeric entity (code point > 0x10FFFF) never throws — stripHtml leaves it as-is instead of crashing a tool call"
    - "An RSS feed whose content:encoded element carries an XML attribute yields the element's text body as item.text, never the string [object Object]"
    - "Every command documented in CLAUDE.md actually exists (npm scripts and referenced files verified against the repo)"
  artifacts:
    - "shared/contract.js — both numeric-entity replacers use guarded String.fromCodePoint"
    - "test/contract.test.js — astral-plane and out-of-range entity assertions"
    - "servers/rss/server.js — content:encoded routed through textOf() (WR-01 pattern)"
    - "test/rss.test.js — object-shaped content:encoded fixture test"
    - "CLAUDE.md — packaging references corrected, inspect command matches package.json"
    - "package.json — inspect:hn script added"
  key_links:
    - "mapRssItem -> textOf -> buildListEnvelope -> normalizeItem -> stripHtml (the text field's full path; both fixes sit on it)"
    - "CLAUDE.md Commands section <-> package.json scripts (must agree)"
---

<objective>
Fix the two Known Bugs and the CLAUDE.md drift documented in `.planning/codebase/CONCERNS.md`:

1. `stripHtml` in `shared/contract.js` mangles astral-plane numeric entities (emoji) because `String.fromCharCode` truncates code points > 0xFFFF.
2. `mapRssItem` in `servers/rss/server.js` reads `content:encoded` raw, bypassing the `textOf()` object-collapsing that every other text-bearing field gets (same bug class already fixed for `author` as WR-01) — an attribute-carrying element renders as `"[object Object]"`.
3. CLAUDE.md documents a `build-mcpb.sh` script that exists nowhere in the repo and an `npm run inspect:hn` script absent from `package.json`.

Purpose: eliminate cosmetic text corruption shipped to the `medium-blog-pro` skill and stop the project docs from sending contributors to nonexistent files/commands.
Output: patched `shared/contract.js` + `servers/rss/server.js` with regression tests, corrected `CLAUDE.md`, and a real `inspect:hn` npm script.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/codebase/CONCERNS.md
@shared/contract.js
@servers/rss/server.js
@test/contract.test.js
@test/rss.test.js
@package.json
@CLAUDE.md

Environment note (CONCERNS.md line 176): `node_modules` may not be installed in this checkout — run `npm ci` before trusting test results. All tests are offline (Node built-in test runner, injected fetch, fixtures).

Verified facts (do not re-derive):
- `textOf(v)` (servers/rss/server.js lines 79-83) returns `null` for nullish, `v["#text"] ?? null` for objects, and `v` unchanged for strings — so routing a plain-string `content:encoded` through it is behavior-preserving, and existing tests at test/rss.test.js lines 52-79 (which assert string `content:encoded` passthrough) still pass.
- `test/rss.test.js` lines 85-114 contain the WR-01 object-coercion tests for `dc:creator` / Atom `author.name` — the new `content:encoded` test mirrors that exact pattern.
- No `build-mcpb.sh` exists under any `servers/*/` directory (glob-verified). `servers/hn/manifest.json` itself states .mcpb packing is deferred to v2 (PKG-01).
- `package.json` `scripts` contains only `"test": "node --test"`.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Decode astral-plane numeric entities with guarded fromCodePoint in stripHtml</name>
  <files>shared/contract.js, test/contract.test.js</files>
  <behavior>
    - Test 1: stripHtml("&#128512;") returns the grinning-face emoji U+1F600 (a single code point, not two mangled BMP chars)
    - Test 2: stripHtml("Launch &#x1F680; now") returns "Launch " + U+1F680 rocket + " now" (hex form, and case-insensitivity of the existing /gi flag still holds for e.g. &#X1F680;)
    - Test 3: stripHtml("bad &#1114112; entity") does NOT throw and leaves the out-of-range reference untouched in the output (0x110000 is above the Unicode maximum; String.fromCodePoint would throw RangeError on it)
    - Test 4: existing BMP assertions (test/contract.test.js lines 128-131: "&#65;" -> "A", "&#x42;" -> "B") still pass unchanged
  </behavior>
  <action>
    In shared/contract.js, fix the two numeric-entity replacers at lines 98-99 of stripHtml(). Currently both use String.fromCharCode, which truncates code points above 0xFFFF. Replace with String.fromCodePoint, guarded so stripHtml can never throw (a thrown RangeError here would hard-error every tool call whose source text contains a malformed entity — violating the project's "a tool call never hard-errors on a blip" resilience constraint).

    Implementation shape: add a small module-private helper (e.g. `decodeCodePoint(match, cp)`) above stripHtml that returns `String.fromCodePoint(cp)` when `Number.isInteger(cp) && cp >= 0 && cp <= 0x10FFFF`, and returns the original `match` string otherwise. Wire both replacers through it: the decimal replacer passes `Number(n)`, the hex replacer passes `parseInt(h, 16)`. Keep the module dependency-free (zod is the only existing import) and preserve replacer order (the &#x27;/&#39; apostrophe special-case at line 96 runs before the generic replacers — leave it).

    Write the tests FIRST (RED), in the existing "--- stripHtml ---" block of test/contract.test.js (after line 131), covering the four behaviors above. Use \u{1F600}/\u{1F680} escapes or literal emoji in expected values — either is fine, the file is UTF-8. Then apply the contract.js fix (GREEN).

    Do not touch itemShape, TYPE, normalizeItem, or any envelope builder — this change is confined to entity decoding inside stripHtml.
  </action>
  <verify>
    <automated>node --test test/contract.test.js</automated>
  </verify>
  <done>All contract tests pass; the new astral tests fail before the fix and pass after; stripHtml("&#128512;") === "\u{1F600}"; out-of-range entity input returns without throwing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route content:encoded through textOf() in mapRssItem (WR-01 pattern)</name>
  <files>servers/rss/server.js, test/rss.test.js</files>
  <behavior>
    - Test 1: mapRssItem with `"content:encoded": { "#text": "<p>Full <b>body</b> &amp; more</p>", "@_type": "html" }` yields text === "<p>Full <b>body</b> &amp; more</p>" (the #text string, raw — HTML stripping happens downstream in normalizeItem, mirroring the existing WR-01 tests at test/rss.test.js lines 90-101)
    - Test 2: mapRssItem with an object-valued content:encoded that has attributes but no "#text" key falls back to textOf(item.description)
    - Test 3 (end-to-end string sanity): buildListEnvelope over the Test 1 item produces results[0].text with tags stripped and "&amp;" decoded — i.e. a plain human-readable string, proving the "[object Object]" failure mode is closed at the envelope level
    - Regression: existing tests at test/rss.test.js lines 52-79 (string content:encoded passthrough and description fallback) still pass unchanged
  </behavior>
  <action>
    In servers/rss/server.js mapRssItem (line 152), change the text mapping from reading item["content:encoded"] raw to routing it through textOf(), matching every other text-bearing field: `text: textOf(item["content:encoded"]) ?? textOf(item.description) ?? null`. This is the exact WR-01 pattern already applied to author at line 145 (per commit 62fee1e) — extend the WR-01 comment above the author field, or add a short one on the text line, noting content:encoded gets the same object-collapse treatment.

    Write the tests FIRST (RED) in test/rss.test.js, placed with the existing WR-01 block (after line 113). Import buildListEnvelope from shared/contract.js for Test 3 if not already imported (check the import list at the top — mapRssItem is imported at line 28). Then apply the one-line server fix (GREEN).

    Behavior-preservation check: textOf returns strings unchanged, so the fixture-based assertions (line 63: text equals the fixture's string content:encoded) must not need editing. If any existing test needs modification, stop — that signals the fix is wrong.
  </action>
  <verify>
    <automated>node --test test/rss.test.js</automated>
  </verify>
  <done>All RSS tests pass; object-shaped content:encoded maps to its #text string; attribute-only object falls back to description; envelope-level text is a readable string, never a stringified object.</done>
</task>

<task type="auto">
  <name>Task 3: Fix CLAUDE.md drift — packaging script and inspect:hn command</name>
  <files>CLAUDE.md, package.json</files>
  <action>
    Two verified drifts (CONCERNS.md "Tech Debt" items 2-3):

    (a) `npm run inspect:hn` is documented in CLAUDE.md's Commands section but package.json scripts contains only "test". Fix by making the doc TRUE: add `"inspect:hn": "npx @modelcontextprotocol/inspector node servers/hn/server.js"` to package.json scripts (the exact fix approach CONCERNS.md line 23 recommends). Do not add sibling inspect scripts for the other eight servers unless it is a one-line-each addition you can verify by name against the actual `servers/` directory listing — if added, each must point at a server.js that exists.

    (b) CLAUDE.md references a packaging shell script that does not exist anywhere in the repo (glob-verified), while servers/hn/manifest.json itself states .mcpb packing is deferred to v2 (PKG-01). Fix by making the doc match reality:
    - In "How to add a new server" step 6: keep the manifest.json + user_config instruction, but drop the instruction to add the packaging script; append a note that .mcpb packing itself is deferred to v2 (PKG-01) and manifests are documentation/scaffold for now.
    - In the Commands block: remove the `cd servers/<name> && ./build-...` line entirely (or replace it with a comment noting packaging is deferred to v2 / PKG-01).

    Use Edit with scoped replacements on CLAUDE.md — do not rewrite the whole file. Leave every other section (output contract, shared modules, conventions, Don'ts) untouched.
  </action>
  <verify>
    <automated>node -e "const s=require('./package.json').scripts; if(!s['inspect:hn'] || !s['inspect:hn'].includes('servers/hn/server.js')) { console.error('inspect:hn script missing/wrong'); process.exit(1); } console.log('ok')"</automated>
  </verify>
  <done>package.json has a working inspect:hn script matching the documented command; CLAUDE.md no longer instructs contributors to create or run the nonexistent packaging script, and marks .mcpb packing as deferred to v2 (PKG-01); every remaining command in CLAUDE.md's Commands block corresponds to a real script or real files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream HTML/XML -> stripHtml/textOf | Untrusted source markup (RSS bodies, forum posts) flows through the decoders being modified |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-quick-01 | DoS | stripHtml numeric-entity replacers | low | mitigate | Guard fromCodePoint against out-of-range code points so malformed entities in untrusted feed content cannot throw and hard-error tool calls (Task 1 Test 3 asserts no-throw) |

No new dependencies are installed and no credential or network surface changes; the existing entity-expansion bounds in servers/rss/server.js (fast-xml-parser processEntities tuning) are untouched.
</threat_model>

<verification>
1. `npm ci` if node_modules is absent (CONCERNS.md notes 11/13 test files fail solely on missing deps in this checkout).
2. `node --test` — full suite (94+ tests, all offline) passes, including the new astral-entity and content:encoded tests.
3. `node -e` package.json script check from Task 3 passes.
4. Read CLAUDE.md "How to add a new server" step 6 and the Commands block: no reference remains to a packaging script as an existing file; .mcpb packing marked deferred (PKG-01).
</verification>

<success_criteria>
- Both Known Bugs from .planning/codebase/CONCERNS.md are fixed with regression tests that fail on the old code.
- The full offline test suite passes via `node --test`.
- CLAUDE.md's documented commands and add-a-server steps match the actual repo contents (package.json scripts, servers/ tree).
- Output contract untouched: no schema field renamed/dropped; only decoding internals and one field-mapping line changed.
</success_criteria>

<output>
Create `.planning/quick/260708-h5g-fix-concerns-md-bugs-astral-entity-decod/260708-h5g-SUMMARY.md` when done.
</output>
