---
phase: 06-author-blog-awareness
plan: 03
type: execute
wave: 2
depends_on: [06-02]
files_modified:
  - servers/rss/server.js
  - test/rss.test.js
  - test/fixtures/substack-archive.json
  - test/fixtures/substack-archive-loginhtml.json
  - test/fixtures/rss-substack-feed.xml
  - docs/AUTHOR-BLOG-RECIPES.md
autonomous: true
requirements: [ABLOG-03, ABLOG-05]
must_haves:
  truths:
    - "rss_substack_archive('pub') fills score from archive reactions and num_comments from archive comment counts (D-09)"
    - "On ANY archive failure (endpoint gone, HTML-200 login page via the content-type gate, throttle) the tool returns the <pub>.substack.com/feed RSS window and never hard-errors (D-10)"
    - "The archive getJson call passes untrustedHost:true so the publication host rides the Phase 5 assertSafeUrl guard; a host resolving to 127.0.0.1 / 169.254.169.254 is rejected (D-08)"
    - "The frozen contract holds: reactions/comments ride existing score/num_comments; no new item key; envelopes parse against ListEnvelopeSchema"
    - "docs/AUTHOR-BLOG-RECIPES.md documents cadence + series/follow-up recipes leading with the ~10/~20 window honesty caveat, operating only on created_utc + title/teaser (D-12/D-13)"
  artifacts:
    - "servers/rss/server.js — exported mapSubstackArchiveItem normalizer + registered rss_substack_archive"
    - "docs/AUTHOR-BLOG-RECIPES.md"
    - "test/fixtures: substack-archive.json (success), substack-archive-loginhtml.json (failure body), rss-substack-feed.xml (fallback window)"
  key_links:
    - "rss_substack_archive try/catch -> getText(<pub>.substack.com/feed) RSS fallback (the EXPECTED path, tested first-class)"
    - "getJson(url,{untrustedHost:true}) -> assertSafeUrl + content-type gate; HTML-200 -> terminal -> caught -> RSS fallback"
---

<objective>
Add `rss_substack_archive(publication)` (ABLOG-03): list a Substack publication's
full archive via the unofficial `https://<pub>.substack.com/api/v1/archive` JSON on
the Phase 5 guarded path `getJson(url, { untrustedHost: true })`, mapping reactions ->
`score` and comment counts -> `num_comments` on the frozen contract. On ANY archive
failure it degrades to the `<pub>.substack.com/feed` RSS window and never hard-errors
(D-10 — the expected path, tested first-class). Then ship `docs/AUTHOR-BLOG-RECIPES.md`
(ABLOG-05) with the cadence and series/follow-up recipes, leading with the honest
window caveat.

Purpose: the one archive-enrichment tool + the documented recipes complete the
writer-aware surface. The publication host is user-supplied, so this is the reuse
SEC-01 was built for.
Output: one registered tool + a pure archive normalizer + offline fixtures/tests
(success, failure->RSS fallback, SSRF reject) + the recipes doc.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-author-blog-awareness/06-CONTEXT.md
@.planning/phases/06-author-blog-awareness/06-PATTERNS.md
@.planning/phases/05-guarded-json-path-trending-signals/05-CONTEXT.md
@servers/rss/server.js
@servers/lemmy/server.js
@shared/http_client.js
@shared/contract.js
@test/rss.test.js
</context>

## Artifacts this plan produces

NEW symbols in `servers/rss/server.js`:
- `mapSubstackArchiveItem(archivePost)` — exported pure normalizer mapping one
  archive JSON post onto a raw contract item; reactions -> `score`,
  comment count -> `num_comments`, `type: "article"` (D-09). Exact archive JSON key
  names are confirmed against a live probe / the fixture (CONTEXT "Claude's Discretion");
  the contract mapping is locked.
- registered tool `rss_substack_archive` (guarded JSON + graceful RSS fallback).

NEW file: `docs/AUTHOR-BLOG-RECIPES.md` (ABLOG-05).
NEW fixtures: `test/fixtures/substack-archive.json`, `substack-archive-loginhtml.json`,
`test/fixtures/rss-substack-feed.xml`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Export mapSubstackArchiveItem normalizer (reactions -> score, comments -> num_comments)</name>
  <files>servers/rss/server.js, test/rss.test.js, test/fixtures/substack-archive.json</files>
  <read_first>
    - servers/rss/server.js:127-158 (mapRssItem — the field-map shape to mirror; note
      score/num_comments are hard-null there, lines 147-148)
    - shared/contract.js:45-57 (itemShape) and :121-140 (normalizeItem / buildListEnvelope)
    - .planning/phases/06-author-blog-awareness/06-CONTEXT.md §D-08, §D-09
    - .planning/phases/06-author-blog-awareness/06-PATTERNS.md — "rss_substack_archive handler"
  </read_first>
  <behavior>
    - mapSubstackArchiveItem(post) -> a raw contract item: type 'article'; id a string;
      title/author/created_utc/url/permalink from the archive post; tags[] present;
      score = the post's reaction total (number or null); num_comments = the post's
      comment count (number or null); text the post's subtitle/description if present.
    - A post missing reactions/comments yields score/num_comments null (not 0), per `??`.
    - The built envelope over the success fixture parses against ListEnvelopeSchema.
  </behavior>
  <action>
    Add and `export` `mapSubstackArchiveItem(post)` to `servers/rss/server.js`,
    modeled on `mapRssItem` (server.js:134-158) but filling the two fields RSS cannot:
    map the archive post's reaction total to `score` (number, else null) and its
    comment count to `num_comments` (number, else null) using `??` so a legitimate 0
    survives and absence -> null. `type: "article"`; `id` = String(post id/slug/url);
    `title` from the post title; `author` from the post's author/byline if present
    (else null); `created_utc` from the post's publish timestamp via the existing
    `toIso` helper; `url`/`permalink` from the canonical post URL; `tags` from the
    post's tags if present (else []); `text` from the subtitle/description. Confirm the
    EXACT archive JSON key names against a live probe of `https://<pub>.substack.com/api/v1/archive`
    (or synthesize the fixture to the shape you map) and note the confirmed keys in a
    code comment — the contract mapping (reactions->score, comments->num_comments) is
    locked; the field names are yours to confirm. Keep the helper pure.

    Create `test/fixtures/substack-archive.json` mirroring the archive shape you map
    (>=2 posts, at least one with reactions + comments and one without). Add tests:
    `mapSubstackArchiveItem` over each fixture post asserts the field map incl.
    numeric score/num_comments (and null when absent); a built `buildListEnvelope`
    over the mapped posts parses against `ListEnvelopeSchema` and preserves count.
  </action>
  <acceptance_criteria>
    - `mapSubstackArchiveItem` is exported; maps reactions -> numeric `score` and
      comment count -> numeric `num_comments`, and yields null (not 0) when absent.
    - `type` is 'article'; id is a string; the envelope over substack-archive.json
      parses against `ListEnvelopeSchema` with `count === results.length`.
    - The confirmed archive key names are documented in a code comment.
    - `node --test test/rss.test.js` passes.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/rss.test.js</automated>
  </verify>
  <done>The archive normalizer fills score/num_comments from reactions/comments on the frozen contract and is unit-tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Register rss_substack_archive (guarded getJson + RSS fallback) + SSRF-reject + finalize registration smoke</name>
  <files>servers/rss/server.js, test/rss.test.js, test/fixtures/substack-archive-loginhtml.json, test/fixtures/rss-substack-feed.xml</files>
  <read_first>
    - servers/lemmy/server.js:149-168 (the untrustedHost:true guarded getJson call — exact analog)
    - shared/http_client.js:289-386 (getJson: untrustedHost path + content-type gate at 327-335)
    - shared/http_client.js:522-599 (getText — the RSS fallback path)
    - test/lemmy.test.js:191-244 (SEC-01 guarded-path test pattern: jsonRes shim,
      publicLookup/privateLookup, driving getJson at the layer since the handler has no seam)
    - test/rss.test.js:312-321 (registration smoke to finalize to exactly four tools)
    - .planning/phases/06-author-blog-awareness/06-CONTEXT.md §D-08, §D-09, §D-10
    - .planning/phases/05-guarded-json-path-trending-signals/05-CONTEXT.md §D-06 (accepted DNS-rebinding residual)
  </read_first>
  <behavior>
    - rss_substack_archive({publication:'pub'}) on a JSON-200 archive -> envelope of
      mapSubstackArchiveItem results with score/num_comments filled; source 'rss'.
    - On archive getJson throw (any cause: 4xx/5xx, HTML-200 caught by the content-type
      gate, network) -> the tool fetches getText('https://pub.substack.com/feed'),
      normalizes, and returns THAT envelope; it never throws.
    - The archive getJson call passes untrustedHost:true; a publication whose host
      resolves to a private/loopback/metadata IP is rejected on the guarded path.
    - The registration smoke now lists exactly the four rss tools, each with outputSchema.
  </behavior>
  <action>
    Add a `server.registerTool("rss_substack_archive", ...)` block. `inputSchema:
    { publication: z.string() }`; `outputSchema: listEnvelopeShape`. Handler: resolve
    the publication to its `<pub>.substack.com` host (reuse the Substack-extraction
    logic from `resolveAuthorFeed`, accepting a bare `pub`, `pub.substack.com`, or a
    full Substack URL), then `try`: `getJson(\`https://<pub>.substack.com/api/v1/archive\`,
    { untrustedHost: true })` (untrustedHost is MANDATORY — user-supplied host, D-08),
    map each archive post through `mapSubstackArchiveItem`, build+return the envelope
    via `buildListEnvelope({ source: SOURCE, query: publication, results }) ->
    toolResult`. `catch (err)`: fall back to `getText(\`https://<pub>.substack.com/feed\`)
    -> parseFeed -> normalizeFeed`, map through the preview-marker helper (06-02), and
    return THAT envelope. NEVER re-throw from the catch (D-10 — archive breakage is the
    expected case). Do NOT catch an SSRF `assertSafeUrl` rejection into a silent success:
    the fallback getText also rides `assertSafeUrl`, so a blocked host is rejected on
    BOTH paths (a private-IP publication ends in a thrown SSRF error, not a fake
    envelope). Write the description in the RECIPE- style: state it lists the FULL
    archive (score/comments enrichment) vs the ~20-item RSS window; note the endpoint
    is unofficial and falls back to the RSS window on failure; add a D-12 recipe pointer.

    Create `test/fixtures/substack-archive-loginhtml.json` is not JSON-shaped — instead
    represent the failure as a fixture/HTML body the test feeds to a jsonRes shim with
    an `text/html` content-type (drives the content-type gate), and
    `test/fixtures/rss-substack-feed.xml` as the fallback RSS window. Add tests (mirror
    the lemmy SEC-01 pattern — drive `getJson`/`getText` at the layer with injected
    `fetchImpl`/`sleep`/`lookup`, since the handler builds opts internally):
    (a) archive JSON-200 success -> mapSubstackArchiveItem envelope, score/num_comments filled;
    (b) archive HTML-200 (content-type gate) -> getJson rejects -> the fallback getText
    over rss-substack-feed.xml yields a contract-valid envelope (assert the fallback
    envelope, proving no hard-error — the EXPECTED path);
    (c) SSRF reject: `getJson(archiveUrl, { untrustedHost:true, lookup: privateLookup })`
    and literal `http://127.0.0.1`/`http://169.254.169.254` hosts reject with /blocked address/;
    Finalize the registration smoke (test/rss.test.js:314-317) to assert the sorted
    names are exactly `["rss_author_posts", "rss_fetch", "rss_substack_archive", "rss_tag_posts"]`,
    each with an `outputSchema`.
  </action>
  <acceptance_criteria>
    - `rss_substack_archive` is registered with `{ publication }` inputSchema + outputSchema.
    - Success test: a JSON-200 archive yields an envelope whose items carry numeric
      score/num_comments (from mapSubstackArchiveItem) and parses against ListEnvelopeSchema.
    - Fallback test: an archive failure (HTML-200 via the content-type gate) results in
      the RSS-window envelope from getText('.../feed') and NO thrown error — asserted first-class.
    - SSRF test: the archive getJson with a private/loopback/metadata host (privateLookup
      and literal 127.0.0.1 / 169.254.169.254) rejects with /blocked address/; untrustedHost:true is set.
    - Registration smoke asserts exactly the four rss tools, each with an outputSchema.
    - `node --test test/rss.test.js` passes.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/rss.test.js</automated>
  </verify>
  <done>rss_substack_archive enriches from the guarded archive JSON, degrades to the RSS window without hard-erroring, and rejects private-host publications.</done>
</task>

<task type="auto">
  <name>Task 3: Write docs/AUTHOR-BLOG-RECIPES.md (cadence + series/follow-up recipes, honest-window first)</name>
  <files>docs/AUTHOR-BLOG-RECIPES.md</files>
  <read_first>
    - servers/rss/server.js:234-245 (the in-code RECIPE- block tone to mirror in prose)
    - .planning/phases/06-author-blog-awareness/06-CONTEXT.md §D-12, §D-13 and <specifics>
    - .planning/research/PITFALLS.md §"Pitfall 2" (truncated paywalled text) and §"UX Pitfalls"
    - docs/ARCHITECTURE.md (house doc tone/structure reference)
  </read_first>
  <behavior>
    - The doc leads with the D-13 honesty caveat before any recipe.
    - It documents a posting-cadence recipe (using created_utc only) and a
      series/follow-up-detection recipe (using title + teaser only).
    - It references rss_author_posts, rss_tag_posts, rss_substack_archive by name and
      states the ~10 Medium / ~20 Substack windows + teaser-quality text.
    - It prescribes no new tool logic, no scraping, no cookie/subscription workarounds.
  </behavior>
  <action>
    Create `docs/AUTHOR-BLOG-RECIPES.md` (ABLOG-05). Open with a prominent honesty
    caveat (D-13): cadence/series conclusions from a feed reflect ONLY the ~10 most
    recent Medium posts / ~20 Substack posts (or the full archive via
    rss_substack_archive) — never lifetime history — and feed `text` is teaser-quality
    (paywalled bodies are abstracts; items tagged `preview-only` are truncated). Then
    two worked recipes operating on NORMAL tool output only: (1) posting-cadence
    tracking — sort items by `created_utc`, compute inter-post gaps, and explicitly
    warn a windowed feed cannot show a slowdown/lifetime rate; recommend
    rss_substack_archive for Substack full history. (2) series / follow-up detection —
    cluster by `title` + teaser (`text`) similarity, noting titles carry most of the
    signal because paid bodies are truncated. State the boundaries: no scraping, no
    cookie/subscription workarounds, no new fields — use only `created_utc`, `title`,
    `text`, `tags` (incl. `preview-only`), `score`, `num_comments`. Mirror the concise
    RECIPE- tone of the in-code blocks. Confirm the in-tool-description recipe pointers
    (added in 06-02 Task 2/3 and 06-03 Task 2) reference this file by name (D-12 —
    recipes in BOTH places); if any pointer is missing or names the file differently,
    reconcile the filename here or note the exact pointer text to fix.
  </action>
  <acceptance_criteria>
    - `docs/AUTHOR-BLOG-RECIPES.md` exists and its FIRST content section is the
      ~10/~20 window + teaser-quality honesty caveat (D-13).
    - It contains a cadence recipe (created_utc-based) and a series/follow-up recipe
      (title+teaser-based), each explicitly bounded to the window and teaser text.
    - It names rss_author_posts, rss_tag_posts, and rss_substack_archive.
    - It states no-scraping / no-cookie-workaround / no-new-field boundaries.
    - The tool descriptions' D-12 recipe pointers reference this exact filename.
  </acceptance_criteria>
  <verify>
    <automated>test -f docs/AUTHOR-BLOG-RECIPES.md &amp;&amp; grep -qiE 'most recent|~?10|~?20' docs/AUTHOR-BLOG-RECIPES.md &amp;&amp; grep -q 'rss_substack_archive' docs/AUTHOR-BLOG-RECIPES.md &amp;&amp; echo OK</automated>
  </verify>
  <done>The recipes doc exists, leads with the honesty caveat, documents both recipes on normal output only, and is cross-referenced from the tool descriptions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tool input `publication` -> getJson(untrustedHost) | The one NEW externally-facing surface this phase adds: an LLM-supplied publication resolves to a `<pub>.substack.com` host fetched via `getJson(url, { untrustedHost: true })`. |
| archive failure -> getText fallback | The fallback also fetches a user-derived `<pub>.substack.com/feed` host through `getText`'s unconditional SSRF guard. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-06 | Tampering (SSRF) | rss_substack_archive getJson host | high | mitigate | `untrustedHost: true` routes the archive call through the SAME `assertSafeUrl` denylist + per-hop redirect re-validation Phase 5 shipped (SEC-01); the getText fallback rides it too. A private/loopback/metadata publication is rejected on BOTH paths — confirmed (not re-derived) here. Tested with privateLookup + literal 127.0.0.1 / 169.254.169.254. |
| T-06-07 | Information disclosure | content-type gate on archive 200 | medium | mitigate | An HTML-200 login/interstitial is caught by getJson's content-type gate (http_client.js:327-335) as terminal, then caught by the handler -> RSS fallback — never `JSON.parse`-crashed and never surfaced as a raw body (D-10). |
| T-06-08 | Denial of service | archive endpoint unavailability | low | accept | The `/api/v1/archive` endpoint is undocumented (RESEARCH LOW tier). Breakage is the EXPECTED case: graceful RSS-window fallback, never a hard error (D-10). No retry added around the failure. |
| SEC-03 | Tampering (DNS rebinding TOCTOU) | assertSafeUrl check-vs-connect gap | low | accept | The check-vs-connect DNS-rebinding residual (no IP pinning) remains accepted for a LOCAL, single-user, personal-use tool — per 05-CONTEXT §D-06 and STATE "Accepted risk". Tracked as SEC-03 (v2+); no change this phase. |
| T-06-SC | Tampering | npm/pip/cargo installs | high | accept | No package installs — zero new runtime dependencies (the archive is a keyless JSON GET through the existing getJson). No `[ASSUMED]`/`[SUS]` packages to gate. |
</threat_model>

<verification>
- `node --test test/rss.test.js` passes: archive success (score/num_comments filled),
  archive-failure -> RSS fallback (no hard-error, the expected path), SSRF reject on
  the archive host, and the finalized exactly-four registration smoke.
- `npm test` full suite stays green.
- `docs/AUTHOR-BLOG-RECIPES.md` exists, leads with the honesty caveat, names all three tools.
- Live check (research "Looks Done But Isn't"): verify against a PAYWALLED Substack —
  archive enrichment fills score/comments; forcing an archive failure yields the RSS window.
</verification>

<success_criteria>
- rss_substack_archive enriches score/num_comments on the guarded path and degrades to the RSS window without hard-erroring.
- Private/loopback/metadata publication hosts are rejected (SEC-01 reuse confirmed).
- The recipes doc ships with the D-13 honesty caveat first and both recipes on normal output only.
- Contract stays frozen; exactly four rss tools registered.
</success_criteria>

<output>
Create `.planning/phases/06-author-blog-awareness/06-03-SUMMARY.md` when done.
</output>
