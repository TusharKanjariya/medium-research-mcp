---
phase: 06-author-blog-awareness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/http_client.js
  - shared/credentials.js
  - test/http_client.test.js
autonomous: true
requirements: [ABLOG-01]
must_haves:
  truths:
    - "getText maps a 403 from a medium.com host to a clear tool-level error naming Medium bot-blocking (D-14)"
    - "A 403 is terminal — no retry, no stale — for Medium and non-Medium hosts alike (strict no-4xx-retry preserved)"
    - "A non-Medium 403 keeps the existing generic getText: HTTP 403 message byte-for-byte (host-gated branch)"
    - "The outbound User-Agent identifies the project + repo URL (identified feed-reader UA, D-14)"
  artifacts:
    - "shared/http_client.js — host-gated Medium-403 error-mapping branch inside getText"
    - "shared/credentials.js — userAgent() default (1.0 -> 1.1 bump, D-14)"
    - "test/http_client.test.js — Medium-403 mapping + no-retry + no-stale cases"
  key_links:
    - "getText 4xx terminal branch (http_client.js:566-570) -> new host-gated Medium-403 message; redactUrl preserved so no query-string leak"
---

<objective>
Land the one shared-file coordination point this phase needs (D-14) so the Medium
author/tag feed tools in 06-02 build on a robust, honest Medium fetch path: map a
Medium 403 / anti-bot response to a clear tool-level error, keep the strict
no-4xx-retry policy, and bump the already-identified User-Agent 1.0 -> 1.1.

Purpose: Pitfall 1 — `medium.com/feed/@user` sits behind CDN bot protection that
403s non-browser agents; under the repo's no-4xx-retry policy a bare 403 hard-errors
every Medium call with a misleading message. The identified UA already exists in
`getText`; only the error mapping (host-gated) and an optional version bump are new.

Output: a host-gated 403 branch in `getText`, a UA version bump, and offline tests
proving the mapping, the no-retry, and the no-stale disposition.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-author-blog-awareness/06-CONTEXT.md
@.planning/phases/06-author-blog-awareness/06-PATTERNS.md
@.planning/research/PITFALLS.md
@shared/http_client.js
@shared/credentials.js
</context>

## Artifacts this plan produces

NEW / MODIFIED symbols:
- `shared/http_client.js` — a host-gated 403 branch in `getText`'s 4xx terminal
  handler (no new export; behavior change only).
- `shared/credentials.js` — `userAgent()` default string bumped `1.0` -> `1.1`.
- `test/http_client.test.js` — new offline cases (Medium-403 mapping, no-retry,
  no-stale, non-Medium 403 unchanged).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Map a Medium 403 to a clear tool-level error in getText (host-gated), bump UA to 1.1</name>
  <files>shared/http_client.js, shared/credentials.js</files>
  <read_first>
    - shared/http_client.js:522-599 (getText) — especially the 4xx terminal branch
      at lines 566-570 (the exact append point) and the UA merge at line 537
    - shared/credentials.js:74-81 (userAgent) — the exact one-line string to bump
    - .planning/phases/06-author-blog-awareness/06-PATTERNS.md — section
      "shared/http_client.js — Medium-403 -> clear tool-level error" (the analog + constraint)
    - .planning/research/PITFALLS.md §"Pitfall 1" (source of D-14) and
      §"Security Mistakes" (the no-retry-around-browser-UA rule)
  </read_first>
  <behavior>
    - getText 403 from a host whose hostname ends in `medium.com` -> thrown Error
      whose message names Medium bot-blocking and suggests another network.
    - getText 403 from any other host -> unchanged `getText: HTTP 403 from <origin+path>`.
    - Either way: no retry (fetchImpl called exactly once) and no stale fallback.
    - Error text carries only redactUrl(url) (origin+path) — never the query string.
  </behavior>
  <action>
    In `getText` (shared/http_client.js), inside the existing non-retryable
    terminal branch that currently sets `lastError = new Error("getText: HTTP
    ${status} ...")` (lines 566-570), add a host-gated special case: when
    `status === 403` AND `new URL(url).hostname` ends with `medium.com`, set
    `lastError` to a clearer message — Medium is blocking automated fetches from
    this network and the feed may still work from another network — while STILL
    setting `transientFailure = false` and `break`ing exactly as the existing branch
    does. Keep `redactUrl(url)` in the message (no query-string leak). Do NOT add a
    retry loop, do NOT touch `RETRYABLE_5XX`, the transient-failure gating, or the UA
    merge at line 537 (the identified UA is already sent). Parse the hostname inside
    a try/catch or reuse a URL already parsed so a malformed URL cannot throw here.
    Non-Medium 403s must keep the current message verbatim.

    In `shared/credentials.js` `userAgent()` (lines 79-81), bump the default string
    version token `medium-research-mcp/1.0` -> `medium-research-mcp/1.1`, leaving the
    `(+https://github.com/...)` repo-URL suffix and the `get("userAgent") ||` override
    path unchanged (D-14 — identified feed-reader UA).
  </action>
  <acceptance_criteria>
    - `shared/credentials.js` `userAgent()` returns a string containing
      `medium-research-mcp/1.1` and the repo URL when `MCP_USER_AGENT` is unset.
    - The `MCP_USER_AGENT` override path is untouched (env value still wins).
    - `getText`'s Medium-403 branch is host-gated on a `medium.com` hostname suffix;
      the generic `getText: HTTP ${status}` message is retained for every non-403
      status and for 403 from non-Medium hosts.
    - No new retry, no `RETRYABLE_5XX` change, no stale-on-403; the added branch sets
      `transientFailure = false` and breaks (terminal), same as the sibling branch.
    - The Medium-403 message contains no `?`-query segment of the URL (redactUrl used).
  </acceptance_criteria>
  <verify>
    <automated>node --test test/credentials.test.js</automated>
  </verify>
  <done>UA bumped to 1.1; getText host-gates a Medium 403 to a clear error without introducing retries or stale.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Offline tests — Medium-403 mapping, no-retry, no-stale, non-Medium 403 unchanged</name>
  <files>test/http_client.test.js</files>
  <read_first>
    - test/http_client.test.js — the existing getText suite + the `res()` response
      shim and the injected `fetchImpl`/`sleep`/`lookup` seams already used for
      getText/getJson (mirror them; do NOT hit the network)
    - test/http_client.test.js:437-660 — the assertSafeUrl + guarded getJson cases,
      for the publicLookup/privateLookup + call-count assertion style to copy
    - shared/http_client.js:522-599 (getText) — the branch under test
  </read_first>
  <behavior>
    - RED first: a 403 from a `https://medium.com/feed/@x` host (publicLookup) throws
      a message matching /Medium is blocking/i.
    - A 403 from `https://example.com/...` throws the existing /getText: HTTP 403/ message.
    - Both: injected fetchImpl invoked exactly once (no retry on 4xx).
    - With a stale cache entry pre-seeded for the Medium URL, a 403 still throws
      (no stale served — terminal 4xx).
  </behavior>
  <action>
    Add offline cases to `test/http_client.test.js` driving `getText` with an injected
    `fetchImpl` that returns a 403 response shim, an injected `sleep` (no real wait),
    and a `lookup` that resolves the host to a public IP (reuse the existing
    publicLookup-style helper). Assert: (1) Medium host -> rejects with /Medium is
    blocking/i and the thrown message excludes the query string; (2) non-Medium host
    -> rejects with the existing /getText: HTTP 403/ shape; (3) a call-count spy proves
    fetchImpl ran once (no 4xx retry); (4) after `set(cacheKey, ...)`-seeding a stale
    entry (or driving a prior success then TTL expiry per the file's existing stale
    pattern), the 403 still rejects rather than returning the stale body. Use unique
    `cacheKey`s per case so cache state never leaks across tests.
  </action>
  <acceptance_criteria>
    - `node --test test/http_client.test.js` passes including the new cases.
    - A Medium 403 assertion matches /Medium is blocking/i; a non-Medium 403 matches
      /getText: HTTP 403/.
    - A call-count assertion proves exactly one fetch on a 403 (no retry).
    - A no-stale-on-403 assertion is present and green.
    - No test performs real network or real timers (fetchImpl + sleep injected).
  </acceptance_criteria>
  <verify>
    <automated>node --test test/http_client.test.js</automated>
  </verify>
  <done>Medium-403 mapping, no-retry, and no-stale are proven offline; non-Medium 403 behavior is unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| feed host -> getText | Upstream (Medium CDN) returns a 403 whose handling this plan changes; no new outbound host is introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Information disclosure | getText 403 error text | low | mitigate | Keep `redactUrl(url)` (origin+path only) on the new Medium-403 message — no query-string / credential leak; asserted by test. |
| T-06-02 | Denial of service (self-inflicted IP ban) | getText retry policy | medium | mitigate | Do NOT wrap the browser UA in a retry loop (behaves like the bot it identifies as). 403 stays terminal (no retry, no stale); asserted by a call-count test. |
| T-06-SC | Tampering | npm/pip/cargo installs | high | accept | No package installs in this phase — zero new runtime dependencies (RESEARCH: "zero new runtime deps"). No `[ASSUMED]`/`[SUS]` packages to gate. |
</threat_model>

<verification>
- `node --test test/http_client.test.js` and `node --test test/credentials.test.js` pass.
- `npm test` (full suite) stays green — no existing UA assertion pins `1.0`.
- Manual read: the Medium-403 branch is host-gated and terminal; no retry/stale path touched.
</verification>

<success_criteria>
- Medium 403 -> clear tool-level error; non-Medium 403 unchanged; both terminal.
- UA default is `medium-research-mcp/1.1 (+repo)`.
- All new tests offline and green.
</success_criteria>

<output>
Create `.planning/phases/06-author-blog-awareness/06-01-SUMMARY.md` when done.
</output>
