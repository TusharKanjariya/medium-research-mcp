---
phase: 10-publish-install-path-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - docs/INSTALL.md
  - README.md
  - docs/claude_desktop_config.all-servers.json
autonomous: true
requirements: [PKG-04, DOC-02]
must_haves:
  truths:
    - "npm pack --dry-run lists all 13 bin targets (servers/aggregator/server.js + the 11 servers/<source>/server.js + bin/install.js), all 7 shared/*.js, and docs/INSTALL.md before any publish (SC3, PKG-04/ordering)"
    - "grep finds zero references to claude_desktop_config.all-servers.json in README.md and docs/ after deletion (SC4, DOC-02/adjacency)"
    - "each of the four install paths (one-shot installer, aggregator, per-source npm, GitHub) has a copy-pasteable command block in docs/INSTALL.md (DOC-02/empty)"
    - "npx medium-research-mcp install appears first in docs/INSTALL.md, labeled recommended (D-01, DOC-02/ordering)"
    - "package.json version is 1.2.0 with repository, homepage, and bugs provenance metadata (D-04)"
    - "docs/claude_desktop_config.all-servers.json no longer exists (SC4)"
    - "git history contains no real credential value for any shared/credentials.js ENV_VAR (secret scan PASS) — this gates the 10-02 repo public flip"
  assumptions:
    - "DOC-02/concurrency: parallel or interrupted reads of a static docs file carry no concurrency guarantee — N/A for a docs artifact; flagged, not covered (probe surfaced, deliberately not turned into a truth)"
  artifacts:
    - "package.json (version 1.2.0 + repository/homepage/bugs)"
    - "docs/INSTALL.md (rewritten: four install paths, installer-led)"
    - "README.md (Install quickstart leads with the installer one-liner)"
    - "docs/claude_desktop_config.all-servers.json (DELETED)"
  key_links:
    - "package.json files whitelist <-> all 13 bins present in npm pack --dry-run tarball"
    - "shared/credentials.js ENV_VAR map <-> the names the git-history secret scan checks"
    - "installer default-bin name medium-research-mcp <-> npx medium-research-mcp install documented as the lead path"
  prohibitions:
    - "MUST NOT publish or leave a secret in the npm tarball or git history"
    - "MUST NOT flip the repo public without a passing git-history secret scan"
    - "MUST NOT run npm version minor here (double-tags against the manual INSTALL.md checklist) — edit the version string directly"
---

<objective>
Prepare the already-built package for its first public release: bump the version, add npm provenance metadata, rewrite docs/INSTALL.md around all four install paths (installer-led), retire the temp local-path file, and run the git-history secret scan that gates the irreversible public flip in 10-02.

Purpose: Everything irreversible (npm publish, repo public flip) happens in 10-02. This plan does all the safe, reversible prep and the one HIGH-severity guard (history secret scan) that must pass before that flip.
Output: package.json @ 1.2.0 with metadata, a four-path docs/INSTALL.md, an installer-led README quickstart, the deleted temp file, and a PASS/FAIL secret-scan result for 10-02.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-publish-install-path-docs/10-CONTEXT.md
@.planning/phases/10-publish-install-path-docs/10-RESEARCH.md
</context>

## Artifacts this phase produces (this plan)

- `package.json` — `version` `1.1.0` → `1.2.0` (D-04); NEW keys `repository`, `homepage`, `bugs` (Pitfall 4 fix). NO `license` field here — deferred to the 10-02 operator decision.
- `docs/INSTALL.md` — rewritten: installer-led (D-01), four paths, GitHub `--package` form documented, release checklist version strings reconciled to 1.2.0 (D-05, stays in INSTALL.md).
- `README.md` — Install quickstart leads with `npx medium-research-mcp install`, keeps the pointer to docs/INSTALL.md (D-02).
- `docs/claude_desktop_config.all-servers.json` — DELETED (SC4, D-08).
- (No new symbols/functions — docs + metadata only. No server/tool code changes; the output contract is untouched.)

<tasks>

<task type="tracer">
  <name>Task 1: Version bump 1.2.0 + provenance metadata + tarball verification (PKG-04 prep, SC3)</name>
  <files>package.json</files>
  <read_first>
    - package.json (the file being edited — current version 1.1.0, files whitelist, 13-bin map)
    - .planning/phases/10-publish-install-path-docs/10-RESEARCH.md (npm pack --dry-run evidence: 34 files; Pitfall 3 npm-version double-tag; Pitfall 4 README links)
    - .planning/phases/10-publish-install-path-docs/10-CONTEXT.md (D-03 unscoped name, D-04 version bump, license = deferred operator decision)
  </read_first>
  <action>
    This is the tracer: prove the publish surface is intact end-to-end before any docs work or publish. Edit package.json directly (do NOT run `npm version` — Pitfall 3 double-tags against the manual checklist): set `version` to `1.2.0`. Add three top-level keys after `description`: `repository` = `{ "type": "git", "url": "git+https://github.com/TusharKanjariya/medium-research-mcp.git" }`, `homepage` = `"https://github.com/TusharKanjariya/medium-research-mcp#readme"`, `bugs` = `{ "url": "https://github.com/TusharKanjariya/medium-research-mcp/issues" }` (these make README relative links resolve on npmjs.com — Pitfall 4). Do NOT add a `license` field and do NOT touch the `files` whitelist, `bin` map, or dependencies — license is an IP decision surfaced to the operator at publish time in 10-02. Then run `npm pack --dry-run` and confirm the ship list contains every bin target and the shared modules and INSTALL.md, and contains no secrets/planning/test artifacts. Leave the tarball unpublished — publishing is 10-02's human-gated step.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(p.version!=='1.2.0')throw new Error('version!=1.2.0'); if(!p.repository||!p.homepage||!p.bugs)throw new Error('missing provenance metadata'); if('license' in p)throw new Error('license must stay undefined until the 10-02 operator decision');" && npm pack --dry-run 2>&1 | grep -F -c "servers/aggregator/server.js" | grep -qx 1 && npm pack --dry-run 2>&1 | grep -F -c "bin/install.js" | grep -qx 1</automated>
  </verify>
  <acceptance_criteria>
    - package.json `version` === `"1.2.0"`
    - `repository`, `homepage`, `bugs` keys present and all point at `github.com/TusharKanjariya/medium-research-mcp`
    - no `license` key present in package.json (operator decision deferred to 10-02)
    - `npm pack --dry-run` output includes `servers/aggregator/server.js`, `bin/install.js`, all 11 `servers/<source>/server.js`, all 7 `shared/*.js`, `docs/INSTALL.md`, and `README.md`
    - `npm pack --dry-run` output includes none of: `.env`, `.planning/`, `test/`, `examples/`, `scripts/`
  </acceptance_criteria>
  <reversibility rating="reversible">Metadata + version edits are trivially revertible before publish.</reversibility>
  <done>Version is 1.2.0 with provenance metadata; the dry-run tarball is verified to ship all 13 bins + shared + INSTALL.md and no secret/planning/test files. The publish surface is proven; docs and publish can build on it.</done>
</task>

<task type="auto">
  <name>Task 2: INSTALL.md four-path rewrite (installer-led) + README quickstart + checklist version reconcile (DOC-02)</name>
  <files>docs/INSTALL.md, README.md</files>
  <read_first>
    - docs/INSTALL.md (the file being rewritten — current per-client blocks, credentials table, .mcpb section, Publishing checklist at the bottom)
    - README.md (the Install section quickstart pointing at docs/INSTALL.md)
    - .planning/phases/10-publish-install-path-docs/10-RESEARCH.md (GitHub default-bin rule + both command forms; Code Examples block; Pitfall 3)
    - .planning/phases/10-publish-install-path-docs/10-CONTEXT.md (D-01 installer leads, D-02 stays at docs/INSTALL.md, D-05 checklist stays in INSTALL.md)
  </read_first>
  <action>
    Rewrite docs/INSTALL.md so the FIRST documented install path, immediately after the intro, is the one-shot installer: a copy-pasteable `npx medium-research-mcp install` block explicitly labeled the recommended path (D-01), describing auto-detect of Claude Desktop / Cursor / Codex / OpenCode, config backup, non-destructive merge of the `medium-research-all` aggregator entry, and the skippable prompts for LIBRARIESIO_KEY and PRODUCTHUNT_TOKEN. Document the other three paths below it as alternatives, each with its own copy-pasteable command block: (a) aggregator manual entry (single `medium-research-all` config entry — reuse the existing per-client config shapes already in the file), (b) per-source npm (`npx -y medium-research-<source>` — keep the existing Claude Desktop / Cursor / Codex / OpenCode config sections and the credentials reference table), (c) GitHub path documenting BOTH forms: `npx github:TusharKanjariya/medium-research-mcp install` (default bin resolves to bin/install.js) and `npx --package=github:TusharKanjariya/medium-research-mcp medium-research-<source>` for a specific server, with a one-line note explaining the bare github command runs the installer because npm's default-bin rule matches the package name. Keep docs/INSTALL.md at its current path (D-02) and keep the Publishing/release checklist inside it (D-05); in that checklist set every version string to 1.2.0 (packed tarball filename `medium-research-mcp-1.2.0.tgz`, `git tag v1.2.0`), and add the operator steps this release introduces: re-confirm the name is free with `npm view medium-research-mcp version` (expect not-found), flip the repo public + `git push origin master`, and live-verify both the npm and github paths from a scratch dir OUTSIDE the repo tree with a fresh npm cache. Keep the manual `git tag` line (do NOT switch to `npm version minor` — Pitfall 3). In README.md, update the Install section quickstart to lead with the one-liner `npx medium-research-mcp install` and keep the existing pointer to docs/INSTALL.md.
  </action>
  <verify>
    <automated>bash -c 'inst=$(grep -n "npx medium-research-mcp install" docs/INSTALL.md | head -1 | cut -d: -f1); ghp=$(grep -n "npx --package=github:TusharKanjariya/medium-research-mcp" docs/INSTALL.md | head -1 | cut -d: -f1); test -n "$inst" && test -n "$ghp" && [ "$inst" -lt "$ghp" ]' && grep -F -q "v1.2.0" docs/INSTALL.md && grep -F -q "npx medium-research-mcp install" README.md</automated>
  </verify>
  <acceptance_criteria>
    - The `npx medium-research-mcp install` block is the first install path in docs/INSTALL.md and is labeled recommended (its line number precedes the GitHub `--package` block)
    - docs/INSTALL.md contains a copy-pasteable command block for each of the four paths: one-shot installer, aggregator manual entry, per-source npm (`npx -y medium-research-<source>`), GitHub (`npx github:TusharKanjariya/medium-research-mcp install` AND `npx --package=github:TusharKanjariya/medium-research-mcp medium-research-<source>`)
    - the Publishing checklist reads version 1.2.0 (`git tag v1.2.0`, `medium-research-mcp-1.2.0.tgz`) and includes the name-404 re-check, repo public-flip + `git push origin master`, and the clean-dir live-verify of both paths
    - no `npm version minor` command appears in the checklist (manual `git tag` retained)
    - README.md Install quickstart leads with `npx medium-research-mcp install` and still links docs/INSTALL.md
  </acceptance_criteria>
  <done>docs/INSTALL.md documents all four install paths installer-first, the GitHub `--package` gotcha is spelled out, the release checklist targets 1.2.0 with the new flip/verify steps, and README leads with the installer one-liner.</done>
</task>

<task type="auto">
  <name>Task 3: Delete temp local-path file + git-history secret scan (SC4 + HIGH gate for 10-02)</name>
  <files>docs/claude_desktop_config.all-servers.json</files>
  <read_first>
    - shared/credentials.js (the ENV_VAR map — authoritative list of credential env-var names to scan history for)
    - .planning/phases/10-publish-install-path-docs/10-RESEARCH.md (Security Domain; temp file is git-untracked with zero non-planning references; D-08)
    - .planning/phases/10-publish-install-path-docs/10-CONTEXT.md (D-08 delete after confirming no README/INSTALL link)
  </read_first>
  <action>
    First confirm the temp file has no live references: grep README.md and docs/ for `claude_desktop_config.all-servers.json` and confirm zero matches (research verified none exist outside .planning). Then delete `docs/claude_desktop_config.all-servers.json` — it is git-untracked, so a plain filesystem removal is complete; no doc edits chase it. Then run the git-history secret scan that gates 10-02's repo public flip: scan the entire history (`git log -p --all`) for a real (non-placeholder) value assigned to any credential env var from shared/credentials.js — STACKEXCHANGE_KEY, GITHUB_TOKEN, LIBRARIESIO_KEY, PRODUCTHUNT_TOKEN, REDDIT_CLIENT_SECRET, REDDIT_PASSWORD, REDDIT_USERNAME, LEMMY_PASSWORD, LEMMY_USERNAME — and for any committed `.env` file. Treat placeholder values (e.g. `your-...-key`, `your-...-token`, empty, `xxx`) as clean; treat any other assigned value as a HIGH finding. Record the outcome as PASS (no secret found) or FAIL (secret + commit sha) in the plan SUMMARY — a FAIL is a blocking result that MUST halt before 10-02's public-flip checkpoint (do not proceed to flip the repo public).
  </action>
  <verify>
    <automated>bash -c 'test ! -f docs/claude_desktop_config.all-servers.json && [ -z "$(grep -rIl "claude_desktop_config.all-servers.json" README.md docs/ 2>/dev/null)" ] && hits=$(git log -p --all | grep -vE "your-|placeholder|example|xxx|EXAMPLE|<|=\"\"|='"'"''"'"'" | grep -E "(STACKEXCHANGE_KEY|GITHUB_TOKEN|LIBRARIESIO_KEY|PRODUCTHUNT_TOKEN|REDDIT_CLIENT_SECRET|REDDIT_PASSWORD|LEMMY_PASSWORD)[[:space:]]*[:=][[:space:]]*[A-Za-z0-9_-]{8,}" | wc -l); echo "secret-hits=$hits"; [ "$hits" -eq 0 ]'</automated>
  </verify>
  <acceptance_criteria>
    - `docs/claude_desktop_config.all-servers.json` does not exist on disk
    - `grep -rIl "claude_desktop_config.all-servers.json" README.md docs/` returns no files (zero live references, SC4)
    - the git-history secret scan reports `secret-hits=0` for every credential env var and no committed `.env` — recorded as PASS in the SUMMARY
    - if any real credential value is found, the scan is recorded FAIL with the commit sha and 10-02's public flip is blocked
  </acceptance_criteria>
  <reversibility rating="reversible">The temp file is untracked; deletion is local-only and re-creatable. The scan is read-only.</reversibility>
  <done>The temp local-path file is gone with zero dangling references, and the full git history is confirmed free of committed secrets — clearing the HIGH-severity gate for the 10-02 public flip.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local repo → git history (about to become public in 10-02) | every past commit becomes world-readable the instant D-06 flips visibility |
| local files → npm tarball | anything not excluded by the `files` whitelist would ship to the public registry |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-01 | Information Disclosure | git history (all commits, `git log -p --all`) | high | mitigate | Task 3 scans full history for real values of every shared/credentials.js ENV_VAR + committed `.env`; any hit is a blocking FAIL that halts before the 10-02 public flip |
| T-10-02 | Information Disclosure | npm tarball (`npm pack --dry-run`) | high | mitigate | Task 1 file-list inspection confirms only `servers/`, `shared/`, `bin/`, `docs/INSTALL.md`, `README.md` ship — no `.env`, `.planning/`, `test/` |
| T-10-SC | Tampering | dependency installs | low | accept | No new package is installed this phase; the publish ships previously-vetted `@modelcontextprotocol/sdk`, `fast-xml-parser`, `zod` only — no SLOP/SUS surface added |
</threat_model>

<verification>
- Task 1 automated verify passes (version 1.2.0, metadata present, no license, aggregator + installer in tarball).
- Task 2 automated verify passes (installer leads, GitHub `--package` form present, v1.2.0 in checklist, README quickstart).
- Task 3 automated verify passes (temp file gone, zero references, `secret-hits=0`).
- Full existing test suite still green: `npm test` (docs/metadata-only phase must not regress any bin — network-dependent suites per MEMORY require network on master).
</verification>

<success_criteria>
- package.json @ 1.2.0 with repository/homepage/bugs; tarball proven to ship all 13 bins + shared + INSTALL.md (SC3 verified pre-publish).
- docs/INSTALL.md rewritten installer-first across all four paths; temp file deleted with zero references (SC4).
- git history confirmed secret-free (PASS) — gate cleared for the 10-02 public flip.
</success_criteria>

<output>
Create `.planning/phases/10-publish-install-path-docs/10-01-SUMMARY.md` when done. Record the secret-scan result (PASS/FAIL + any sha) explicitly — 10-02 reads it as the gate for the repo public flip.
</output>
