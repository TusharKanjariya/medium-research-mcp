---
phase: 07-universal-sources-parameterization-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/contract.js
  - test/contract.test.js
  - servers/lemmy/server.js
  - test/lemmy.test.js
autonomous: true
requirements: [SEC-02]
must_haves:
  truths:
    - "shared/contract.js TYPE enum contains 'topic' and 'status' appended AFTER 'launch' (no reorder/removal of the prior twelve)"
    - "Lemmy's three tools accept an optional `instance` parameter that overrides the LEMMY_INSTANCE env default (SC5 — Lemmy instance is a tool parameter, env is optional default only)"
    - "When a tool-param `instance` differs from the LEMMY_INSTANCE env host, the Lemmy request is sent anonymously (no Bearer token replayed to a caller-chosen host)"
  artifacts:
    - "shared/contract.js (TYPE append)"
    - "servers/lemmy/server.js (normalizeInstance override + host-gated Bearer)"
    - "test/lemmy.test.js (instance override + host-mismatch anon + retained SSRF reject)"
    - "test/contract.test.js (topic/status inclusion assertion)"
  key_links:
    - "Lemmy getJson calls use base = normalizeInstance(instance ?? lemmyInstance()) and keep untrustedHost:true"
    - "Bearer header attached ONLY when effective base === lemmyCreds().instance host"
---

<objective>
Shared foundation for Phase 7: append the two new contract TYPE values the new servers need, and turn Lemmy's instance into a tool parameter without leaking the env Bearer token to a caller-chosen host.

Two independent, disjoint-file changes grouped as the Wave-1 prerequisite:
1. Append `"topic"` (Discourse topics + Mastodon trending tags, D-05/D-09) and `"status"` (Mastodon timeline statuses, D-09) to the append-only `TYPE` enum in `shared/contract.js`. Both new servers depend on these values existing so their `structuredContent` validates.
2. Parameterize Lemmy (D-13/D-15/SEC-02): add an optional `instance` tool param overriding `lemmyInstance()`, reuse the `normalizeInstance` default-scheme helper, and gate the Bearer header so a token minted for the env instance is NEVER sent to a different tool-param host.

Purpose: unblock the parallel Discourse and Mastodon server plans (Wave 2) and land the Lemmy half of the SEC-02 parameterization guarantee.
Output: appended TYPE enum, parameterized Lemmy server, updated offline tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-universal-sources-parameterization-audit/07-CONTEXT.md
@.planning/phases/07-universal-sources-parameterization-audit/07-RESEARCH.md
@.planning/phases/07-universal-sources-parameterization-audit/07-PATTERNS.md
</context>

<artifacts_produced>
## Artifacts this plan produces

- `shared/contract.js`: two new `TYPE` enum values `"topic"`, `"status"` (appended after `"launch"`).
- `servers/lemmy/server.js`: `normalizeInstance()` helper (new in this file); `instance?` param on `lemmy_hot`, `lemmy_search`, `lemmy_post`; host-gated Bearer auth.
- `test/contract.test.js`: assertion that `TYPE` includes `topic` and `status`.
- `test/lemmy.test.js`: instance-override test, host-mismatch-anonymous test, retained SSRF-reject test.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Append "topic" and "status" to the contract TYPE enum (D-05, D-09)</name>
  <files>shared/contract.js, test/contract.test.js</files>
  <read_first>
    - shared/contract.js (lines 21-41 — the append-only TYPE enum and its append-only contract comment)
    - test/contract.test.js (lines 65-116 — the Phase 3 additive-extension test pattern to mirror)
    - .planning/phases/07-universal-sources-parameterization-audit/07-PATTERNS.md (§"shared/contract.js (MODIFY)")
  </read_first>
  <action>
    In shared/contract.js, append exactly two string values to the END of the TYPE array, immediately after the `"launch"` entry: `"topic"` (with a trailing comment noting Discourse topics + Mastodon trending tags, Phase 7 SRC-10/SRC-11, D-05/D-09) and `"status"` (comment: Mastodon timeline statuses, Phase 7 SRC-11, D-09). Do NOT reorder, rename, or remove any existing value — this is an append-only change validated by toolResult()/ItemSchema per the file's own contract note. Do NOT add a value for Mastodon trending links: they reuse the existing `"article"` type (D-09).
    In test/contract.test.js, mirror the existing "TYPE includes the Phase 3 additive values" test with a new test asserting TYPE includes `"topic"` and `"status"`, and confirm the prior-values additive test still holds.
  </action>
  <verify>
    <automated>node --test test/contract.test.js</automated>
  </verify>
  <acceptance_criteria>
    - TYPE array's last two elements are `"topic"` then `"status"`, both after `"launch"`.
    - The prior twelve TYPE values are unchanged in value and order (existing additive test passes).
    - `test/contract.test.js` asserts `TYPE.includes("topic")` and `TYPE.includes("status")`.
    - `node --test test/contract.test.js` exits 0.
  </acceptance_criteria>
  <done>Contract exposes the two new append-only type values; contract tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Parameterize the Lemmy instance and host-gate the Bearer token (D-13, D-15, SEC-02)</name>
  <files>servers/lemmy/server.js</files>
  <read_first>
    - servers/lemmy/server.js (whole file — current base = lemmyInstance() at lines 150/186/221; conditional Bearer wire lines 98-111; getJson calls already pass untrustedHost:true at 157-160/194-197/224-233)
    - shared/credentials.js (lines 83-96 lemmyInstance(); lines 118-123 lemmyCreds() — the env host the JWT is minted for)
    - .planning/phases/07-universal-sources-parameterization-audit/07-RESEARCH.md (§"Runtime State Inventory" Lemmy auth-vs-param note; §"Code Examples" normalizeInstance; Open Question 3)
    - .planning/phases/07-universal-sources-parameterization-audit/07-PATTERNS.md (§"servers/lemmy/server.js (MODIFY)")
  </read_first>
  <action>
    Add a `normalizeInstance(instance)` helper to servers/lemmy/server.js per 07-RESEARCH.md §Code Examples: trim the input, default a missing scheme to `https://`, strip trailing slashes, throw a readable error on empty input. Use ONLY generic wording in that error (no specific forum-host literal) — see the security note below.
    Add `instance: z.string().optional()` to the inputSchema of all three tools (`lemmy_hot`, `lemmy_search`, `lemmy_post`) alongside the existing fields, and update each handler's signature to destructure `instance`.
    In each handler, replace `const base = lemmyInstance();` with `const base = normalizeInstance(instance ?? lemmyInstance());` so a tool-param instance wins and the env value remains the optional default. Keep every getJson call's existing `untrustedHost: true` flag unchanged (it already rides the SEC-01 guard — do not remove it, do not add a bare getJson).
    Host-gate the Bearer (D-15 security sub-decision, Open Question 3): attach the Authorization header ONLY when the effective `base` host equals `lemmyCreds()?.instance` host (the host the JWT was minted for). When a tool-param `instance` is provided that differs from LEMMY_INSTANCE, send anonymous (empty headers) — never replay the env token to a caller-chosen host. Implement this by making the auth-header resolution take the effective base and compare normalized hosts before calling lemmyAuthHeaders(); when hosts mismatch, use `{}`.
    Update each tool description to state that `instance` is an optional per-call override of the LEMMY_INSTANCE default and that authenticated reads apply only when the instance matches the env-configured LEMMY_INSTANCE. Preserve the existing LEMMY_INSTANCE auth note (the lemmy tests assert its presence).
    SECURITY (do not miss): do not embed any specific Lemmy forum host as a string literal anywhere except the existing `programming.dev` env default (which is allowlisted); the SEC-02 audit (plan 07-04) forbids non-allowlisted host literals in server source.
  </action>
  <verify>
    <automated>node --test test/lemmy.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `lemmy_hot`, `lemmy_search`, `lemmy_post` each declare `instance: z.string().optional()` in inputSchema.
    - With no `instance` arg, base resolves to `normalizeInstance(lemmyInstance())` (default programming.dev preserved).
    - With an `instance` arg, that value (scheme-defaulted, trailing-slash-stripped) is the outbound base.
    - Every Lemmy getJson call still passes `untrustedHost: true`.
    - The Bearer header is present only when the effective base host === lemmyCreds().instance host; a differing tool-param instance yields empty (anonymous) headers.
    - No specific Lemmy forum host literal exists in server.js other than the `programming.dev` env default.
  </acceptance_criteria>
  <done>Lemmy instance is a tool parameter over the guarded path; env token never leaves its own host.</done>
</task>

<task type="auto">
  <name>Task 3: Extend Lemmy offline tests for the instance param, host-mismatch anon, and SSRF reject (D-15)</name>
  <files>test/lemmy.test.js</files>
  <read_first>
    - test/lemmy.test.js (whole file — reuse the jsonRes shim lines 202-209, publicLookup/privateLookup 210-211, and the SEC-01 guarded-path tests 213-244)
    - servers/lemmy/server.js (the updated exports from Task 2)
    - .planning/phases/07-universal-sources-parameterization-audit/07-PATTERNS.md (§"test/discourse.test.js + test/mastodon.test.js" SEC-01 guarded-path section for the injection pattern)
  </read_first>
  <action>
    Add tests to test/lemmy.test.js that exercise the new instance parameterization without live network. Cover: (a) the host-gated auth decision — assert the pure/injectable auth resolution returns the Bearer fragment when the effective base host matches lemmyCreds().instance and returns `{}` when a tool-param instance host differs (drive the exported header/decision seam directly, mirroring the existing bearerHeaders/lemmyAuthHeaders unit tests); (b) `normalizeInstance` defaults a scheme-less instance to https and strips a trailing slash, and throws on empty input; (c) retain and, if needed, generalize the existing SEC-01 guarded-path tests so a private-IP instance (privateLookup) is rejected with `/blocked address/` and a public instance builds a valid envelope — these now stand for the user-supplied instance param path.
    Keep all tests offline: inject fetchImpl/sleep/lookup/cacheKey into getJson exactly as the existing SEC-01 tests do; do not add a live network call.
  </action>
  <verify>
    <automated>node --test test/lemmy.test.js</automated>
  </verify>
  <acceptance_criteria>
    - A test asserts Bearer is sent when instance host matches the env host and anonymous ({}) when it differs.
    - A test asserts normalizeInstance scheme-defaulting, trailing-slash strip, and empty-input throw.
    - A private-IP instance on the guarded path is rejected with `/blocked address/`; a public instance yields a schema-valid list envelope.
    - `node --test test/lemmy.test.js` exits 0.
  </acceptance_criteria>
  <done>Lemmy parameterization and its guard behavior are proven offline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP caller → Lemmy tool `instance` param | Untrusted host string now selects the outbound base (previously env-only) |
| Lemmy server → arbitrary instance host | Env-minted Bearer token must not cross to a caller-chosen host |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-03 | Information disclosure | Lemmy Bearer sent to tool-param instance ≠ env host | high | mitigate | Attach Authorization only when effective base host === lemmyCreds().instance host; anonymous otherwise (Task 2); asserted by Task 3 host-mismatch test |
| T-07-01 | Information disclosure (SSRF) | Lemmy getJson with user-supplied `instance` | high | mitigate | Keep `untrustedHost:true` on all four calls (assertSafeUrl denylist + redirect re-validation); private-IP reject test in Task 3 |
| T-07-05 | Tampering | TYPE enum reorder/removal breaking existing servers' output validation | medium | mitigate | Append-only change; additive test in Task 1 asserts prior values unchanged |
| T-07-SC | Tampering | package installs | low | accept | Zero new dependencies this plan/phase (RESEARCH §Standard Stack) — no install surface |
| T-04-06 | Information disclosure | DNS-rebinding TOCTOU on guarded path | low | accept | Accepted residual for a local single-user tool (SEC-03, re-affirmed Phase 5); not reopened |
</threat_model>

<verification>
- `node --test test/contract.test.js` and `node --test test/lemmy.test.js` both exit 0.
- `node --test` (full suite) exits 0 — the TYPE append and Lemmy changes do not regress any existing server test.
</verification>

<success_criteria>
- TYPE enum exposes `topic` and `status` (append-only), unblocking Wave-2 servers.
- Lemmy instance is a tool parameter over the SEC-01 guarded path; the env Bearer is never sent to a mismatched host (SC5, Lemmy half).
</success_criteria>

<output>
Create `.planning/phases/07-universal-sources-parameterization-audit/07-01-SUMMARY.md` when done.
</output>
