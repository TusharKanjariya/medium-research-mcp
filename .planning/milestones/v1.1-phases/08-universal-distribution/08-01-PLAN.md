---
phase: 08-universal-distribution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - shared/main.js
  - servers/hn/server.js
  - servers/stackexchange/server.js
  - servers/lobsters/server.js
  - servers/lemmy/server.js
  - servers/devto/server.js
  - servers/github/server.js
  - servers/librariesio/server.js
  - servers/producthunt/server.js
  - servers/rss/server.js
  - servers/discourse/server.js
  - servers/mastodon/server.js
autonomous: true
requirements:
  - PKG-02
user_setup: []

must_haves:
  truths:
    - "npx medium-research-<source> runs any of the 11 servers on Windows and Unix with no bundler (shebang-first ESM, type:module preserved) — PKG-02"
    - "package.json bin has exactly 11 entries named medium-research-<source>, each pointing at servers/<source>/server.js"
    - "The npm tarball ships servers/ AND shared/ (npm pack --dry-run lists both); test/ and .planning/ are excluded (D-07, guards Pitfall 9 ERR_MODULE_NOT_FOUND on npm)"
    - "package.json version is 1.1.0 and the private flag is removed, so npm publish is possible (publish itself is the user's manual gate, D-06)"
  artifacts:
    - "package.json — bin map (11), files whitelist, version 1.1.0, private removed, @anthropic-ai/mcpb@2.1.2 devDependency, build:mcpb + example:sweep scripts"
    - "shared/main.js — isEntry(importMetaUrl) realpath-hardened entry guard helper"
  key_links:
    - "each server.js: #!/usr/bin/env node shebang (line 1) + isEntry(import.meta.url) guard so the file both bin-executes and auto-connects ONLY when it is the entry point"
---

<objective>
Turn the repo into one publishable, npx-runnable npm package (PKG-02): package name
`medium-research-mcp` (unscoped, D-08), version `1.1.0` (D-10), one `bin` entry per
server named `medium-research-<source>` (D-09), a `files` whitelist that ships both
`servers/` and `shared/` (D-07/Pitfall 9), Windows-safe `#!/usr/bin/env node` shebangs,
and a shared realpath-hardened entry guard so bin execution is universal.

Purpose: SC2 — a user can run any server via `npx` on any non-Claude MCP client, on
Windows, with no build step. This plan owns `package.json` entirely and all 11
`server.js` files so later plans never contend on them.

Output: modified `package.json`, new `shared/main.js`, 11 shebang+guard-swapped
`server.js` files. NO publish, NO tag — those are the user's manual gate (D-06).
</objective>

## Artifacts this phase produces

New files/symbols across ALL of Phase 8 (listed here so source-grounding excludes them
from drift verification): `scripts/build-mcpb.mjs`, `.mcpbignore`, `docs/INSTALL.md`,
`examples/pain-point-sweep.mjs`, `shared/main.js` (`isEntry`), `test/manifest-consistency.test.js`,
`dist/` (gitignored build output). New `package.json` fields: `bin` (11 entries), `files`
whitelist, `build:mcpb` + `example:sweep` scripts, `@anthropic-ai/mcpb@2.1.2`
devDependency, version 1.1.0, `private` removed. The 11 bin names:
`medium-research-{hn,stackexchange,lobsters,lemmy,devto,github,librariesio,producthunt,rss,discourse,mastodon}`.
Modified: all 11 `servers/*/server.js` (shebang + isEntry guard), all 11
`servers/*/manifest.json` (version + entry_point/args retarget; hn user_config cleanup),
`README.md` (INSTALL link), `.gitignore` (dist/).

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-universal-distribution/08-RESEARCH.md
@package.json
@servers/hn/server.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite package.json for one npx-runnable package (bin + files + version)</name>
  <read_first>
    - package.json (the artifact this task rewrites)
    - .planning/phases/08-universal-distribution/08-RESEARCH.md §"package.json changes (PKG-02)" and §"Pattern 2" (bin map, files whitelist, devDependency)
    - .planning/phases/08-universal-distribution/08-CONTEXT.md D-08/D-09/D-10 (name, bin naming, version/private/files)
  </read_first>
  <files>package.json</files>
  <action>
    Edit package.json to these exact target values, keeping every existing field not
    named here byte-unchanged (name stays `medium-research-mcp`, description stays,
    dependencies stay, `type` stays `module`, engines stays `{ "node": ">=18" }`):
    (1) set `version` to `1.1.0`; (2) DELETE the `"private": true` line entirely;
    (3) add a `files` array = `["servers/", "shared/", "docs/INSTALL.md", "README.md"]`
    — this whitelist MUST include `servers/` and `shared/` (omitting `shared/` ships
    ERR_MODULE_NOT_FOUND per Pitfall 9) and MUST NOT list `test/` or `.planning/`;
    (4) add a `bin` object with exactly these 11 keys mapping to server paths:
    `medium-research-hn`→`servers/hn/server.js`,
    `medium-research-stackexchange`→`servers/stackexchange/server.js`,
    `medium-research-lobsters`→`servers/lobsters/server.js`,
    `medium-research-lemmy`→`servers/lemmy/server.js`,
    `medium-research-devto`→`servers/devto/server.js`,
    `medium-research-github`→`servers/github/server.js`,
    `medium-research-librariesio`→`servers/librariesio/server.js`,
    `medium-research-producthunt`→`servers/producthunt/server.js`,
    `medium-research-rss`→`servers/rss/server.js`,
    `medium-research-discourse`→`servers/discourse/server.js`,
    `medium-research-mastodon`→`servers/mastodon/server.js`;
    (5) in `scripts`, keep `test` and `inspect:hn`, and add `build:mcpb` =
    `node scripts/build-mcpb.mjs` and `example:sweep` = `node examples/pain-point-sweep.mjs`
    (the referenced files are created by plans 03 and 04 — forward references are fine
    because those plans run in a later wave); (6) add a `devDependencies` object with
    `@anthropic-ai/mcpb` pinned to exactly `2.1.2` (devDependency ONLY — never a runtime
    dependency; do not touch `dependencies`). Then run `npm install` once so the
    lockfile records @anthropic-ai/mcpb (dev-time install, not a publish).
  </action>
  <acceptance_criteria>
    - `node -e "const p=require('./package.json'); const b=Object.keys(p.bin); if(b.length!==11) throw new Error('bin count '+b.length); if(!b.every(k=>/^medium-research-[a-z]+$/.test(k))) throw 'bin name'; if(p.private!==undefined) throw 'private present'; if(p.version!=='1.1.0') throw 'version'; if(!p.files.includes('servers/')||!p.files.includes('shared/')) throw 'files missing servers/shared'; if(p.files.includes('test/')) throw 'test leaked'; if(p.devDependencies['@anthropic-ai/mcpb']!=='2.1.2') throw 'mcpb dep'; if(p.type!=='module') throw 'type'; console.log('OK')"` prints `OK`
    - `npm pack --dry-run` output lists `servers/` and `shared/` paths and does NOT list `test/` or `.planning/`
    - `dependencies` is unchanged (still `@modelcontextprotocol/sdk`, `fast-xml-parser`, `zod`); `@anthropic-ai/mcpb` is absent from `dependencies`
  </acceptance_criteria>
  <verify>
    <automated>node -e "const p=require('./package.json'); const b=Object.keys(p.bin); if(b.length!==11) throw new Error('bin='+b.length); if(p.private!==undefined) throw new Error('private'); if(p.version!=='1.1.0') throw new Error('ver'); if(!p.files.includes('servers/')||!p.files.includes('shared/')) throw new Error('files'); if(p.devDependencies['@anthropic-ai/mcpb']!=='2.1.2') throw new Error('mcpb'); console.log('OK')"</automated>
    <automated>npm pack --dry-run 2>&1 | grep -E "servers/|shared/" | head</automated>
  </verify>
  <done>package.json ships as one unscoped npm package: 11 medium-research-* bins, files whitelist including servers/ + shared/, version 1.1.0, private removed, @anthropic-ai/mcpb@2.1.2 devDependency, forward-referenced build:mcpb + example:sweep scripts.</done>
</task>

<task type="auto">
  <name>Task 2: Add shared isEntry() guard + shebang/guard-swap across all 11 servers</name>
  <read_first>
    - servers/hn/server.js (canonical main-guard at the file tail: the `import.meta.url === pathToFileURL(process.argv[1]).href` block — every server copies this)
    - .planning/phases/08-universal-distribution/08-RESEARCH.md §"Pitfall B" (naive guard fails under symlinked installs; realpathSync fix) and §"Pattern 2" (shebang)
    - shared/credentials.js (confirm shared/ module style: ESM named exports, JSDoc header)
  </read_first>
  <files>shared/main.js, servers/hn/server.js, servers/stackexchange/server.js, servers/lobsters/server.js, servers/lemmy/server.js, servers/devto/server.js, servers/github/server.js, servers/librariesio/server.js, servers/producthunt/server.js, servers/rss/server.js, servers/discourse/server.js, servers/mastodon/server.js</files>
  <action>
    Create `shared/main.js` exporting one function `isEntry(importMetaUrl)` that returns
    true when the given `import.meta.url` is the process entry. It resolves
    `process.argv[1]` to its real path with `realpathSync` before comparing (via
    `pathToFileURL`), so the guard is TRUE for both copy installs (npx/registry/Windows
    `cmd /c npx`) AND symlinked installs (pnpm / `npm link`) — the naive guard silently
    fails the symlink case (Pitfall B). Guard against a missing `process.argv[1]` (return
    false) and wrap `realpathSync` so a resolution error falls back to comparing the raw
    `process.argv[1]` (never throw at import time). Import `realpathSync` from `node:fs`
    and `pathToFileURL` from `node:url`.

    Then in EACH of the 11 `servers/<name>/server.js` apply the SAME two mechanical edits
    (the rest of the file stays byte-identical — this is Option A's byte-preserving
    property): (a) make the FIRST line of the file exactly `#!/usr/bin/env node` (a single
    shebang line, before the leading `//` comment banner); (b) replace the existing
    entry-guard block — the `if ( process.argv[1] && import.meta.url === pathToFileURL(process.argv[1]).href )`
    conditional wrapping `await server.connect(new StdioServerTransport())` — with
    `if (isEntry(import.meta.url)) { await server.connect(new StdioServerTransport()); }`,
    add `import { isEntry } from "../../shared/main.js";` to the import block, and remove
    the now-unused `import { pathToFileURL } from "node:url";` line IF that server used
    pathToFileURL only for the guard (hn also uses it nowhere else — check each file; keep
    the import only if the module still references pathToFileURL elsewhere). Do NOT touch
    any tool registration, handler, mapping helper, or output — the frozen contract and
    tool surface must not change (this edit is process lifecycle only).
  </action>
  <acceptance_criteria>
    - `node -e "const {isEntry}=await import('./shared/main.js'); if(typeof isEntry!=='function') throw 'no isEntry'; if(isEntry('file:///nonexistent-not-entry.js')) throw 'false-positive'; console.log('OK')" --input-type=module` prints `OK`
    - `head -1` of every `servers/*/server.js` is exactly `#!/usr/bin/env node` (11/11)
    - `node --check` passes for all 11 `servers/*/server.js` (no syntax error after edits)
    - every `servers/*/server.js` imports `isEntry` from `../../shared/main.js` and calls `isEntry(import.meta.url)`; no `server.js` still contains the literal `import.meta.url === pathToFileURL`
    - `npm test` (or per-file `node --test test/hn.test.js`) still passes — the existing server tests import these modules without starting a transport, proving the guard swap did not regress
  </acceptance_criteria>
  <verify>
    <automated>node --input-type=module -e "const {isEntry}=await import('./shared/main.js'); if(typeof isEntry!=='function') throw new Error('no isEntry'); if(isEntry('file:///nope.js')) throw new Error('fp'); console.log('OK')"</automated>
    <automated>for f in servers/*/server.js; do test "$(head -1 "$f")" = '#!/usr/bin/env node' || { echo "NO SHEBANG: $f"; exit 1; }; node --check "$f" || exit 1; done; echo "shebang+syntax OK (11)"</automated>
    <automated>node --test test/hn.test.js test/discourse.test.js test/mastodon.test.js</automated>
  </verify>
  <done>shared/main.js exports a realpath-hardened isEntry(); all 11 servers start with the node shebang and connect only via isEntry(import.meta.url); tool surface and output contract unchanged; existing tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → npm registry tarball | `files` whitelist decides exactly what bytes get published; anything not excluded ships publicly |
| published package → client `node` | npx spawns `node servers/<name>/server.js` from the tarball; the shebang + guard decide whether it connects |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | Information disclosure | `files` whitelist in package.json | high | mitigate | Allowlist = servers/, shared/, docs/INSTALL.md, README.md only — no `.env`, no `.planning/`, no `test/`; verified by `npm pack --dry-run` inspection (D-07) BEFORE the user's manual publish |
| T-08-02 | Elevation of privilege | bin shebang / entry guard | low | accept | Shebang is inert `#!/usr/bin/env node`; isEntry() only gates stdio connect, adds no new surface; frozen tool set unchanged |
| T-08-SC | Tampering | `@anthropic-ai/mcpb` devDependency | high | mitigate | Pinned to exactly 2.1.2 (verified official Anthropic maintainers, RESEARCH Package Legitimacy Audit); devDependency ONLY so it is never vendored into a shipped bundle nor the runtime dep tree |
</threat_model>

<verification>
- `npm pack --dry-run` file list contains `servers/` and `shared/`, excludes `test/` and `.planning/`.
- All 11 `servers/*/server.js` begin with `#!/usr/bin/env node`, pass `node --check`, and import `isEntry` from `../../shared/main.js`.
- Existing server tests still pass (guard swap is non-regressive).
- `dependencies` unchanged; `@anthropic-ai/mcpb` present only under `devDependencies`.
</verification>

<success_criteria>
PKG-02 satisfied: one unscoped `medium-research-mcp` package at v1.1.0 with 11
`medium-research-<source>` bins, a `files` whitelist shipping servers/ + shared/,
Windows-safe shebangs, and a universal isEntry() guard — `npx medium-research-<source>`
is runnable (publish deferred to the user's manual gate, D-06).
</success_criteria>

<output>
Create `.planning/phases/08-universal-distribution/08-01-SUMMARY.md` when done.
</output>
